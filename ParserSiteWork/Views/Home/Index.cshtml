@model string
@{
    ViewData["Title"] = "Home Page";
}

<h1>Прием Word документов</h1>

@using (Html.BeginForm("DisplayDocumentData", "DataReciever", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input type="file" name="file" />
    <input type="submit" value="Загрузить" />
}
